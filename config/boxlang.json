{
    "modules": {
        "bxai" :{
            "settings" : {
                // The default provider LLM AI: openai, deepseek, etc
                "provider" : "openrouter",
                // The default request params to use when calling a provider, any provider
                // Ex: { temperature: 0.5, max_tokens: 100, model: "gpt-5.6-luna" }
                // gpt-oss-120b
                "defaultParams" : {
                    //"model": "stepfun/step-3.5-flash"
					"model" : "openrouter/free"
					//"model" : "openrouter/auto"
                },
                // The default memory to use when calling aiMemory()
                "memory" : {
                    "provider" : "window",
                    "config" : {}
                },
                // This is used to configure default options and parameters for a specific provider
                // The name of the key is the provider name, each provider can have its own params and options
                "providers" : {
                    "openai" : {
                    	"params" : {
							"model": "gpt-5.6-luna"
						}
                    },
					"claude" : {
						"params": {
							"model": "claude-sonnet-5"
						}
					},
					// See examples/40-bedrock.bxs. region/bearerToken here are optional —
					// omit them entirely to fall back to the default AWS credential chain
					// (AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY env vars, then ECS/EKS
					// container credentials, then EC2 IMDSv2 instance-role credentials).
					"bedrock" : {
						"params" : {
							"model" : "anthropic.claude-3-5-sonnet-20241022-v2:0"
						},
						"options" : {
							"region" : "us-east-1"
							// "bearerToken" : "" // or set AWS_BEARER_TOKEN_BEDROCK instead
						}
					}
                },
                // The default timeout of the ai requests in seconds. Omitted here on purpose
                // to inherit the module's own default (90s) — see bx-ai's ModuleConfig.bx.
                // If you need a shorter/longer timeout for this project, set it explicitly:
                // "timeout" : 90,
                // If true, the AI request will be logged into the ai.log
                "logRequest" : false,
                // If true, the AI request will be logged to the console
                "logRequestToConsole" : false,
                // If true, the AI response will be logged into the ai.log
                "logResponse" : false,
                // If true, the AI response will be logged to the console
                "logResponseToConsole" : false,
                // The default return format of the AI response: single, all, raw
                "returnFormat" : "single"
            },
			// --------------------------------------------------------------------------
			// Skills Directory — Global Skill Auto-Discovery
			// --------------------------------------------------------------------------
			// Path to the directory scanned for SKILL.md files at module startup.
			// Defaults to "/{webroot}/.agents/skills" — a vendor-neutral convention that works with
			// any AI toolchain.  Can be absolute or relative (relative paths are
			// expanded from the CWD).  Set to "" to disable auto-discovery entirely.
			"skillsDirectory" : "/.agents/skills",

			// When true, skills found in skillsDirectory are automatically loaded and injected
			// into every agent created with aiAgent() as global / shared skills.
			"autoLoadSkills" : true,

			// Internal — populated by onLoad() with the globally discovered skills.
			// Access via aiGlobalSkills() with no arguments, or read directly from settings.
			"globalSkills" : [],

			// --------------------------------------------------------------------------
			// Audio Settings — Defaults for aiSpeak() / aiTranscribe() / aiTranslate()
			// --------------------------------------------------------------------------
			"audio" : {
				"defaultVoice"              : "openai",
				"defaultOutputFormat"       : "mp3",
				// Uses the defaults from the provider config (e.g. model: gpt-4o-mini-tts) if not overridden here
				"defaultSpeechModel"        : "",
				"defaultTranscriptionModel" : ""
			},

			// --------------------------------------------------------------------------
			// Security Settings — Prompt-injection guardrails (see examples/34-security-guardrails.bxs)
			// --------------------------------------------------------------------------
			// enabled: false by default here — every example that demonstrates a security
			// middleware attaches it per-request instead, so this project can also show what
			// happens WITHOUT it. Flip enabled to true to auto-attach InputSanitizerMiddleware/
			// fencing to EVERY chat request (aiChat, aiModel, aiAgent) project-wide.
			"security" : {
				"enabled" : false,
				"input" : {
					"enabled" : true,
					"action" : "flag",
					"detectors" : [],
					"customPatterns" : [],
					"normalizeUnicode" : true,
					"stripZeroWidth" : true,
					"scanToolResults" : true
				},
				"fencing" : {
					"enabled" : true,
					"fenceContext" : true,
					"escapeBindings" : true,
					"preamble" : ""
				}
			},

			// --------------------------------------------------------------------------
			// Gateway Settings — per-gateway configuration for aiGateway( name ) (see examples/35-gateways.bxs, examples/37-gateway-sessions.bxs)
			// --------------------------------------------------------------------------
			// The name of the key is the gateway name; each gateway has its own options.
			// Ex: { "slack": { "botToken": "...", "signingSecret": "..." } }
			"gateways" : {
			},

			// --------------------------------------------------------------------------
			// HITL Settings — default decision store for approve_always/approve_session
			// durable grants (see examples/36-hitl-batching-and-grants.bxs)
			// --------------------------------------------------------------------------
			"hitl" : {
				"decisionStore" : {
					"provider" : "cache",
					"config" : {}
				}
			}
        }
    }
}
